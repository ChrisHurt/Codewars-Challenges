Array.prototype.sameStructureAs = function (other) {
    // Recursively call the function to access every single value that is present
      // Valid end points are
        // any value
        // empty array
        // empty object
    // Record the accesspath as a string for each endpoint as elements of an array
    // Iterate through the array and attempt to access each endpoint.
    // If all pass, it has the same structure
    // If any fail, it does not have the same structure
};
